using NLog;
using Tyranny.Networking;

namespace GameClientLib.PacketHandlers
{
    class Movement : IPacketHandler
    {
        private static Logger logger = LogManager.GetCurrentClassLogger();

    }
}
